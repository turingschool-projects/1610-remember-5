import Ember from 'ember';

export default Ember.Route.extend({
  

  model() {
    // const reminders = this.get('store').findAll('reminder')
    // return this.get('store').findAll(reminder => reminder.id === params.reminder_id)
  }
});
