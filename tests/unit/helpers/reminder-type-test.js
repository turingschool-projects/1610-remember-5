
import { reminderType } from 'remember/helpers/reminder-type';
import { module, test } from 'qunit';

module('Unit | Helper | reminder type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reminderType([42]);
  assert.ok(result);
});

